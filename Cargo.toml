[package]
name = "hex-arm-dynamics"
version = "0.1.0"
edition = "2021"
description = "串联机械臂动力学(从 URDF):正运动学 + 重力力矩 G(q)。仅依赖 urdf-rs,手写线代(无 nalgebra)。供控制器 GRAVITY_COMP、host 端 tau_ff 前馈、仿真重力负载共用同一模型。"

[dependencies]
anyhow = "1"
urdf-rs = "0.9.0"
